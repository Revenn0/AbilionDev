# AbilionDev

Recorte novo do Abilion: login, área interna e criador de funis / fluxo.

O código completo está no workspace deste agente. Faz `git remote add` e push deste recorte para este repositório.

```bash
npm install
cp .env.example .env
npm run dev
```
