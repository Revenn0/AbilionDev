# URL do projecto Supabase — só o Worker local (vite) usa isto. O browser não leva chave.
VITE_SUPABASE_URL=https://eyjgmkmaixmpmeeahxon.supabase.co

# Cloudflare Workers — URL deste recorte, nunca o Worker antigo
VITE_APP_URL=
# ABILION_ENV=production já vai no wrangler.jsonc do Worker

# Secrets só no Worker — nunca neste ficheiro
# wrangler secret put ABILION_OPERATOR_PASSWORD  # opcional: só cria contas que ainda não existem
# wrangler secret put TELEGRAM_BOT_TOKEN
# wrangler secret put TELEGRAM_WEBHOOK_SECRET    # POST /api/telegram sem isto é 401
# wrangler secret put SUPABASE_SERVICE_ROLE
# wrangler secret put CRON_SECRET      # obrigatório para GET /api/cron
# wrangler secret put ESTER_CHAT_ID
# wrangler secret put OPENAI_API_KEY    # chave OpenRouter sk-or-v1…
# wrangler secret put OPENCODE_API_KEY  # chave OpenCode oc_sk_… (DeepSeek V4.1 Flash)
# wrangler secret put ELEVENLABS_API_KEY
# wrangler secret put ELEVENLABS_VOICE_ID  # voz clonada da Sté
# Principal: deepseek-v4.1-flash no OpenCode Go
# Reserva:   google/gemma-4-31b-it:free → deepseek/deepseek-v4-flash-0731:free
